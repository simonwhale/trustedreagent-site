# Trusted Agent Site — Launch Checklist

**MANDATORY**: Before declaring any change "live" / "deployed" / "fixed" / "done", run the matching checklist from `/home/user/workspace/skills/launch-checklist/`.

## Site-specific gates

### Toolkit form (`/toolkit/`)
After ANY change, run:
```bash
bash /home/user/workspace/skills/launch-checklist/scripts/verify-brevo-form.sh \
  --form-url "https://thetrustedagent.co.uk/toolkit/" \
  --proxy-url "https://www.relocationagentnetwork.com/api/toolkit-lead" \
  --notify-url "https://www.relocationagentnetwork.com/api/toolkit-notify" \
  --brevo-key "$BREVO_API_KEY"
```
Must exit 0 before saying it's live.

### Lookup page (`/lookup/`)
- [ ] Verify postcode lookup works for known winner (e.g. NE2 2DN → Sarah Mains)
- [ ] Verify postcode lookup correctly says "no match" for non-winner

### Static page changes (any HTML)
- [ ] git push completed
- [ ] Wait 60s for GitHub Pages
- [ ] curl -sI https://thetrustedagent.co.uk/<path> returns 200
- [ ] Open in browser, take screenshot, eyeball it
- [ ] Test mobile width (375px) AND desktop (1280px)
- [ ] If hero image changed: cache-bust with ?v=N

### CSS/visual changes
- [ ] Screenshot all 5 main pages at desktop and mobile
- [ ] Verify Snitcher script still present
- [ ] Verify Plausible script still present

## Snitcher / Plausible regression
After ANY layout-touching change:
```bash
for url in / /toolkit/ /lookup/ /methodology/ /kerfuffleconference/; do
  echo "=== $url ==="
  curl -sL "https://thetrustedagent.co.uk$url" | grep -oE "(snitcher|sOYa6Wlpyd|plausible)" | sort -u
done
```
Each URL must show snitcher + plausible.

## Toolkit asset changes (logos, certificates, social cards)
- [ ] Test the in-page certificate generator for "Test Agency" name
- [ ] Test all 5 download buttons produce a PNG that opens
- [ ] Visual diff vs previous version

## When in doubt
Read `/home/user/workspace/skills/launch-checklist/SKILL.md`.
